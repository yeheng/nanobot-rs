name: "test_echo"
description: "Echoes back the input (simple mode)"
version: "1.0.0"
parameters:
  type: object
  properties:
    message: { type: string, description: "Message to echo" }
  required: ["message"]
runtime:
  command: "python3"
  args: ["simple_echo/echo.py"]
  timeout_secs: 10
